---
name: invest-analyst
description: 按"结论先行+预期差驱动"范式为给定标的生成深度投研报告（单文件 HTML 报表）。当用户要求"分析/研究某公司或股票并输出投研报告"、"按此模式/模板分析 XX 公司"时使用。
agent_created: true
---

# 投研报告生成（结论先行 + 预期差驱动）

## Overview

本 skill 提供一套完整的投研报告生成方法论框架（源自腾讯控股深度报告案例）：给定一家公司 → 收集多维数据 → 按"结论先行+预期差驱动"范式生成**标准格式 HTML 报表**（用户默认输出格式，除非明确要求 markdown）。

核心范式：

> **结论先行 → 多维事实铺底 → 归因诊断 → 量化框架定标 → 预期差提炼 → 时间分层结论 → 反向检验收尾**

## 工作流程（生成模式）

1. **收集标的多维数据**（行情/财务/估值/股东/业务分部，优先用行情数据服务；港股注意财年口径与港元换算）
2. **套用模板**：复制 `assets/report_template.html` 为「{{COMPANY}}_投研分析报告_YYYYMMDD.html」，替换全部占位符（模板头部注释列明完整占位符清单与章节结构）
3. **标注已定价程度**：每条投资逻辑必须标注"部分定价/低/几乎未定价/充分定价"，用 `.logic .priced` 标签展示
4. **反向检验收尾**：必须有"反向检验/自我拷问"，对抗确认偏误，用 `.warn` 区块展示
5. **交付**：保存后用 present_files 展示 HTML 报表（自动开预览）

**数据获取经验（westock-mcp 一条链路）**：
- `data_quote` → 行情/估值快照（含 PE/PB/股息率/52周区间/YTD）
- `data_finance` (income) → 多年财务（注意财年与累计口径；单季数据 = 年度减前三季累计推算）
- `data_finance type=cashflow` 不返回资本开支（仅利润表字段），capex 需标注"需核实"
- `data_kline` (month) → 月K收盘价（画走势图）
- `data_shareholder` → 机构持仓 + 十大股东
- `data_report` → 券商研报列表（标题含评级 tzpj，可直接引用）
- `data_news` → 个股新闻（找催化剂）
- PE 历史序列无接口 → 用"区间示意条"（.pe-band）展示历史高低点与当前分位

## 七大核心方法

1. **结论先行法** — TL;DR 开篇即给"一句话本质+基本面+估值+核心分歧+赔率"
2. **多维交叉验证法** — 行情/财务/估值/股东/业务五维互证，注意口径调整
3. **历史纵深视角法** — 十年时间轴识别结构性变化 vs 周期性波动
4. **权重归因法** — 对现象的归因必须按影响权重排序，核心归因决定分析重点
5. **正反方对照法** — 看多逻辑与反方声音并列，反方要具体、可证伪
6. **量化框架评估法** — OEQ 五因子打分 + 公式化输出，不能只有形容词
7. **时间分层法** — 短期(6-12月)/中期(1-3年)分开看逻辑、风险、观察信号

两个隐性方法：
- **数据来源透明**：标注"需核实原文"、"非一手来源"
- **反向检验法**：对多头自我拷问（"如果今天有一笔新资金，你还买它吗？"）

## OEQ 五因子框架

| 因子 | 含义 | 评分方向 |
|------|------|----------|
| ROE | 盈利质量 | 高为优，注意去投资化口径 |
| 现金回报率 (1-d+d/PB) | 股东回报 | d=分红率，PB 高则折价 |
| moat | 护城河 | 网络效应/生态闭环 |
| cont | 连续性 | 现金流/收入比、经营危机史 |
| dupont penalty | 杠杆惩罚 | 权益乘数、负债结构 |

隐含长期回报 = ROE × (1-d+d/PB) × (moat·cont·dupont 系数)

## 输出格式（固定）：单文件 HTML 报表（标准格式 v2）

以 `assets/report_template.html` 为基础，结构固定为：

1. **头部 KPI 卡**（6 张：最新价/市值/PE/PB/股息率/年内涨跌，涨红跌绿）
2. **TL;DR 结论先行**（米黄块：一句话本质 + 催化剂 + 基本面 + 估值 + 赔率）
3. **章节 1-11（含 3 张内置图表）**：
   - ① 行情速览表 ② 财务全景（**组合图**：营收+净利柱 + 毛利率折线双Y轴 + 年度表 + **最新季度指标表**：营收/经营溢利/归母净利/EPS）③ 业务结构（**doughnut 饼图** + 分部定位表）④ 股价复盘（**月K走势图** + 权重归因）⑤ 核心战略深拆（**投入端/产品矩阵/变现端**三块 + 一句话总结）⑥ 估值（**三维定位表 + PE 历史位置条 + 机构评级表 + 股价已包含什么**）⑦ 股东结构（**十大股东表 + 股权变化解读**）⑧ OEQ 五因子 ⑨ 投资逻辑与预期差（每条标已定价程度 + 共识vs预期差表）⑩ 风险清单（短/中期分层）⑪ 投资结论（分层表 + 反向警示）
4. **免责声明 + 数据来源口径说明**

设计规范（模板内已内置 CSS）：浅色专业风格（深蓝标题栏 #1050b0、米白底、米黄 TL;DR）；中国惯例涨=红/跌=绿；数字用 `.num`；来源标注用 `.mini`；单文件自包含，Chart.js 走 CDN（3 图：finChart 财务组合 / segChart 业务饼图 / kChart 月K走势）。

## Resources

### assets/report_template.html
**HTML 报表模板 v2（生成必用，标准输出格式）**。固定 11 节结构 + 3 张内置 Chart.js 图表（财务组合图/业务饼图/月K走势）+ 全部 CSS（深蓝标题栏、米黄 TL;DR、PE 位置条、涨红跌绿）。占位符替换即用，头部注释列明完整占位符清单。文件命名：`{{公司}}_投研分析报告_YYYYMMDD.html`。

### references/投研报告方法论.md
完整方法论文档：七大方法详解、OEQ 框架、预期差驱动原理、可复用模板。生成报告时按需加载，勿全文加载到 SKILL.md。
